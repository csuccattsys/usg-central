 "use client";
import {useState} from "react"; import {createClient} from "../../lib/supabase"; import {Input,Button,Card} from "../../components/ui"; import {useRouter} from "next/navigation";
export function LoginForm(){const[e,setE]=useState("");const[p,setP]=useState("");const[err,setErr]=useState("");const[load,setLoad]=useState(false);const r=useRouter();async function submit(x:React.FormEvent){x.preventDefault();setLoad(true);setErr("");const {error}=await createClient().auth.signInWithPassword({email:e,password:p});if(error)setErr(error.message);else r.push("/dashboard");setLoad(false)}return <Card className="w-full max-w-md p-7"><div className="mb-6"><div className="text-xs font-semibold uppercase tracking-widest text-[#c8a951]">USG Central</div><h1 className="mt-2 text-2xl font-bold text-[#0b1f3a]">Officer Login</h1></div><form onSubmit={submit} className="space-y-4"><Input type="email" placeholder="Email" required value={e} onChange={x=>setE(x.target.value)}/><Input type="password" placeholder="Password" required value={p} onChange={x=>setP(x.target.value)}/>{err&&<div className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{err}</div>}<Button className="w-full">{load?"Signing in...":"Sign in"}</Button></form></Card>}